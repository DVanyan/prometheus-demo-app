services:
  demo-app:
    build: .
    container_name: prometheus-demo-app
    restart: unless-stopped
    ports:
      - "8000:8000"
    networks:
      - monitoring

networks:
  monitoring:
    external: true
    name: monitoring-lab_default
